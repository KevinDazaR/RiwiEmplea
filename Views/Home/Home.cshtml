@{
    ViewData["Title"] = "Home Page";
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
        crossorigin="anonymous">
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
        }

        .container-flex {
            display: flex;
            flex-direction: column;
            height: 100%;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
        @*background-color:#f8f9fa;border:1pxsolid#dee2e6;*@
        }

        .header .user-info {
            font-size: 1.25rem;
        }

        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .carousel {
            width: 50%;
        }

        .btn-primary {
            color: #fff;
            background-color: #171E4A;
        }
    </style>
</head>

<body class="sb-nav-fixed">
    <div class="container-flex">
        <header class="header">
            <div class="user-info">
                ¡Hola, @User.Identity.Name! 👋
            </div>
            <div class="date-info">
                Bienvenido, hoy es @DateTime.Now.ToString("dd/MM/yyyy")
            </div>
        </header>
        <main class="main-content">

            <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img src="/images/banner.jpg" class="object-fit-sm-contain border rounded w-100" alt="...">
                    </div>
                    <div class="carousel-item">
                        <img src="/images/banner.jpg" class="object-fit-sm-contain border rounded w-100" alt="...">
                    </div>
                    <div class="carousel-item">
                        <img src="/images/banner.jpg" class="object-fit-sm-contain border rounded w-100" alt="...">
                    </div>
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls"
                    data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls"
                    data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
            </div>

            <div class="mt-4 text-center">
                <form asp-route="####" method="post">
                    <button type="submit" class="btn btn-primary">Crea tu hoja de vida aquí</button>
                </form>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"
        crossorigin="anonymous"></script>
</body>

</html>
